// Find the Perimeter of a Rectangle

const findPerimeter = (length, width) => {
  const rectangle = 2 * (length + width);
  return rectangle;
};
const result = findPerimeter(2, 9);
console.log(result);
